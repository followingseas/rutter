# 패키지 기본 values — 프로젝트의 values 파일·--set이 이 값을 덮어쓴다
profile:
  organization: followingseas
  language: ko

security:
  signing:
    # 기본 비활성 (서명 인프라 도입 전). rutter.yaml lockedFields에 등록되어 있어
    # 프로젝트에서 이 값을 바꾸는 upgrade는 --approve-locked-field-change 승인 없이는 실패한다
    required: false
